AgriSecure static front-end for GitHub Pages

This folder contains the static HTML/CSS/JS site served by GitHub Pages.

To publish:
1. Push to the `main` branch.
2. The included GitHub Actions workflow will publish the `docs/` folder to Pages automatically.

Notes:
- This is a static demo only; server-side Laravel features are not available on GitHub Pages.
